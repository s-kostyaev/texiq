(rule
 (targets version.ml)
 (action
  (with-stdout-to
   %{targets}
   (echo "let value = \"%{version:texiq}\""))))

(executable
 (name main)
 (public_name texiq)
 (libraries core core_unix.command_unix texiq)
 (preprocess
  (pps ppx_jane)))
